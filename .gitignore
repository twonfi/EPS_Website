# /
.DS_Store
email-templates/
# this is used for a local testing server, we should still use cf pages --twonfi
site/.htaccess

# /site
site/departments/*
site/clubs.html
site/404.html
site/orca-news/*
site/.wrangler/
# "keepers". they MUST exist, to prevent dir not found failing build
!site/departments/_
!site/orca-news/_